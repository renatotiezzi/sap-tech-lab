@AbapCatalog.viewEnhancementCategory: [#NONE]
@AccessControl.authorizationCheck: #NOT_REQUIRED
@EndUserText.label: 'Value Help Status MGR - APP LR'
@ObjectModel.dataCategory: #VALUE_HELP
@ObjectModel.representativeKey: 'Status'
define view entity ZC_Q2C_STATUS_VH_APP
  as select distinct from ztbq2c_arq_mgr
{
  key status as Status,
      @Semantics.text: true
      case status
        when 'ERRO'             then 'Erro'
        when 'PENDENTE'         then 'Pendente'
        when 'EM_PROCESSAMENTO' then 'Em Processamento'
        when 'PROCESSADO'       then 'Processado'
        when 'CANCELADO'        then 'Cancelado'
        else status
      end as StatusText
}
where status is not null
  and status <> ''