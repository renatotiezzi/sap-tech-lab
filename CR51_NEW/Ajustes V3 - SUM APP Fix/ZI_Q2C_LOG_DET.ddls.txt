@AbapCatalog.viewEnhancementCategory: [#NONE]
@AccessControl.authorizationCheck: #NOT_REQUIRED
@EndUserText.label: 'LOG MGR - Detalhe de Execução (filho do SUM)'
@Metadata.ignorePropagatedAnnotations: true

define view entity ZI_Q2C_LOG_DET
  as select from ztbq2c_log_mgr as log

  association to parent ZI_Q2C_LOG_SUM as _Parent
    on  $projection.Pedido   = _Parent.Pedido
    and $projection.Bandeira = _Parent.Bandeira

{
  key log.pedido   as Pedido,
  key log.bandeira as Bandeira,
  key log.datum    as Datum,
  key log.uzeit    as Uzeit,
      log.id_ref   as IdRef,
      log.etapa    as Etapa,
      log.mensagem as Mensagem,
      log.ernam    as Ernam,

      _Parent
}
